# OIC Core Calculus v1.1 (Elevated)

**Operational Instantiation Calculus** — Lean 4 formal kernel.

```
AGD Corpus → OIC → ThreadLock-RCC → Reality-Coherence
```

- Zero `sorry`
- Zero errors
- Lean 4.29.0 kernel verified
- Bidirectional diagnostic totality
- Dependent `AuthorizedCertificate`
- ThreadLock four-class taxonomy
- Reality-Coherence domain-scoped soundness

Author: James Michael Darnell (JMKK)

## Build

```bash
lake build
```

## CI

GitHub Actions (`lean_verify.yml`) runs `lake build` on every push to Lean sources.
